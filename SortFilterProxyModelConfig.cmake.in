include(CMakeFindDependencyMacro)

if("${QT_MAJOR_VER}" STREQUAL "6")
    find_dependency(Qt6 COMPONENTS Core Qml REQUIRED)
else()
    find_dependency(Qt5 COMPONENTS Core Qml REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/SortFilterProxyModelTargets.cmake")
